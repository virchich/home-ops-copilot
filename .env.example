# =============================================================================
# Home Ops Copilot — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Variables use __ (double underscore) as a nested delimiter.
# See app/core/config.py for all available settings.

# ---------------------------------------------------------------------------
# Required
# ---------------------------------------------------------------------------

# OpenAI API key (required for all features)
OPENAI_API_KEY=sk-...

# ---------------------------------------------------------------------------
# LLM Settings (optional — sensible defaults provided)
# ---------------------------------------------------------------------------

# OpenAI model for generation (default: gpt-5.2)
# LLM__MODEL=gpt-5.2

# Sampling temperature — lower = more deterministic (default: 0.3)
# LLM__TEMPERATURE=0.3

# Max completion tokens (default: 16000)
# LLM__MAX_COMPLETION_TOKENS=16000

# ---------------------------------------------------------------------------
# RAG Pipeline Settings (optional)
# ---------------------------------------------------------------------------

# Number of chunks to retrieve per query (default: 5)
# RAG__TOP_K=5

# Target tokens per chunk (default: 512)
# RAG__CHUNK_SIZE=512

# Minimum similarity score to include a chunk (default: 0.3)
# RAG__MIN_RELEVANCE_SCORE=0.3

# Embedding model (default: text-embedding-3-small)
# RAG__EMBEDDING_MODEL=text-embedding-3-small

# Enable cross-encoder reranking — experimental (default: false)
# RAG__RERANK_ENABLED=false

# ---------------------------------------------------------------------------
# Data Paths (optional — defaults are relative to project root)
# ---------------------------------------------------------------------------

# Directory containing raw PDF documents
# PATHS__RAW_DOCS_DIR=data/raw_docs

# Document metadata JSON file
# PATHS__METADATA_FILE=data/metadata.json

# Persisted vector index directory
# PATHS__INDEX_DIR=data/indexes

# ---------------------------------------------------------------------------
# Observability — Langfuse Tracing (optional)
# ---------------------------------------------------------------------------
# When disabled (default), tracing is a complete no-op with zero overhead.

# Enable Langfuse tracing (default: false)
# OBSERVABILITY__ENABLED=false

# Langfuse credentials (required only when enabled)
# OBSERVABILITY__LANGFUSE_PUBLIC_KEY=pk-lf-...
# OBSERVABILITY__LANGFUSE_SECRET_KEY=sk-lf-...
# OBSERVABILITY__LANGFUSE_BASE_URL=https://us.cloud.langfuse.com

# ---------------------------------------------------------------------------
# App Settings (optional)
# ---------------------------------------------------------------------------

# Enable debug mode (default: false)
# DEBUG=false
