# Local Manuals

This directory is intentionally empty in git.

Add your own equipment manuals as PDF files here when running the app locally, then update `data/metadata.json` so each
`file_name` matches one local PDF.

The repository does not include, redistribute, display, or claim ownership of any equipment manuals. PDF files in this
directory are ignored by git.
